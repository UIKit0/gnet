Source: gnet
Section: libs
Priority: optional
Maintainer: David Helder <dhelder@umich.edu>
Standards-Version: 3.0.1

Package: libgnet@GNET_MAJOR_VERSION@.@GNET_MINOR_VERSION@-dev
Architecture: any
Depends: libgnet@GNET_MAJOR_VERSION@.@GNET_MINOR_VERSION@ (= ${Source-Version}), libc6-dev, libglib1.2-dev
Description: Developer files for GNet network library
 Development files for GNet
 .
 Features:
   * TCP 'client' sockets
   * TCP 'server' sockets
   * Non-blocking TCP sockets
   * UDP
   * IP Multicast
   * Internet address abstraction

Package: libgnet@GNET_MAJOR_VERSION@.@GNET_MINOR_VERSION@
Architecture: any
Depends: libglib1.2
Description: GNet network library
 GNet is a simple network library.  It is writen in C, object-oriented,
 and built upon glib.  It is intended to be small, fast, easy-to-use,
 and easy to port.  The interface is similar to the interface for
 Java's network library.
 .
 GNet is a simple network library.  It is writen in C, object-oriented,
 and built upon glib.  It is intended to be small, fast, easy-to-use,
 and easy to port.  The interface is similar to the interface for
 Java's network library.
 .
 Features:
   * TCP 'client' sockets
   * TCP 'server' sockets
   * Non-blocking TCP sockets
   * UDP
   * IP Multicast
   * Internet address abstraction
